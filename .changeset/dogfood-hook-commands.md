---
"check-my-toolkit": patch
---

Dogfood hook commands in project's own git hooks

- Add commit-msg hook using `cm process check-commit`
- Add check-branch to pre-push hook
- Update check.toml to require these hooks
