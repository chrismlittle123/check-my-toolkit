---
"check-my-toolkit": minor
---

Add monorepo support with `--monorepo` flag for `cm check` command. Detects all projects in a monorepo and runs checks in each project that has a check.toml config, then aggregates results with per-project status and total summary.
