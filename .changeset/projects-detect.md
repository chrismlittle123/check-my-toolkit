---
"check-my-toolkit": minor
---

Add `cm projects detect` command for monorepo project discovery. Detects TypeScript, Python, Rust, and Go projects by marker files, shows which have check.toml configs, and can create missing configs with `--fix`.
