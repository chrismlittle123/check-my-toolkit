---
"check-my-toolkit": patch
---

Remove prompts validation from `cm validate registry` command. The command now only validates rulesets/*.toml files, as prompts are no longer a core feature of the registry.
