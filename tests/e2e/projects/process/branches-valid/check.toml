# Branches validation - valid pattern
# Note: This test will skip in CI due to git branch detection
[process.branches]
enabled = true
pattern = "^(feature|fix|hotfix|docs)/.*"
exclude = ["main", "master", "develop"]
