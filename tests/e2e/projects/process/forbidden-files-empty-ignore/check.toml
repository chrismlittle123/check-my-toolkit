# Test case for #185 - empty ignore array overrides defaults
# With ignore = [], all directories are scanned (nothing ignored)
[process.forbidden_files]
enabled = true
files = ["**/.env"]
ignore = []
